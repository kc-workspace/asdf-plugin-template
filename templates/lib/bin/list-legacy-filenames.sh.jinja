#!/usr/bin/env bash

__asdf_bin() {
  # shellcheck disable=SC2034
  local ns="$1"
  shift

  {%- if version_file_enabled %}

  local files=({{ version_file_names }})
  printf '%s' "${files[*]}"
  {%- endif %}
}
